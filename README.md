# Resume-Generator-HTML-CSV
HTML-CV-Builder-CSV is a Python-based tool that allows you to generate a structured HTML resume from CSV data. This project loads data from CSV files (including personal information, skills, education, experience, achievements, projects, publications, and languages), and dynamically builds an HTML resume with a clean and professional layout.
